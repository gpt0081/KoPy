import contextlib
import io

from kopy.cli import main


def 명령_실행(arguments: list[str]) -> tuple[int, str, str]:
    """main을 호출하고 (종료 코드, stdout, stderr)를 반환하세요."""
    # TODO
    return -1, "", ""
