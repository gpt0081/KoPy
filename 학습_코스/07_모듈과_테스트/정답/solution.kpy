데프 안전_나누기(왼쪽, 오른쪽):
    이프 오른쪽 == 0:
        레이즈 밸류에러("0으로 나눌 수 없습니다.")
    리턴 왼쪽 / 오른쪽


데프 중앙값(값들):
    이프 낫 값들:
        레이즈 밸류에러("값이 필요합니다.")
    정렬값 = 소티드(값들)
    가운데 = 렌(정렬값) // 2
    이프 렌(정렬값) % 2 == 1:
        리턴 정렬값[가운데]
    리턴 (정렬값[가운데 - 1] + 정렬값[가운데]) / 2
