클래스 제한_카운터:
    데프 __init__(self, 최댓값):
        self.최댓값 = 최댓값
        self.값 = 0

    데프 증가(self, 양=1):
        새_값 = self.값 + 양
        이프 새_값 > self.최댓값:
            레이즈 밸류에러("최댓값을 넘을 수 없습니다.")
        self.값 = 새_값
        리턴 self.값

    데프 초기화(self):
        self.값 = 0
