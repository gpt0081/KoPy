# model.onnx 파일이 준비되어 있다고 가정합니다.
임포트 온엑스런타임 애즈 ort
임포트 넘파이 애즈 np

프로바이더 = ort.겟어베일러블프로바이더스()
세션 = ort.인퍼런스세션("model.onnx", providers=프로바이더)

입력이름 = 세션.겟인풋스()[0].name
출력이름 = 세션.겟아웃풋스()[0].name
입력값 = np.어레이([[1.0, 2.0, 3.0, 4.0]], np.플로트32)

출력값 = 세션.런([출력이름], {입력이름: 입력값})[0]
프린트(출력값)
